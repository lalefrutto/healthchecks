{{- if and .Values.basicAuth (not .Values.existingSecret) }}
apiVersion: v1
kind: Secret
metadata:
  name: {{ include "flower.fullname" . }}
  labels:
    {{- include "flower.labels" . | nindent 4 }}
type: Opaque
stringData:
  basic_auth: {{ .Values.basicAuth | quote }}
{{- end }}
