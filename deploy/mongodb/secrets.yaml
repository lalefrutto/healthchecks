# Секретные values MongoDB — ссылки на Vault (secret/mongodb)
auth:
  rootPassword: ref+vault://secret/mongodb#/root_password

customUsers:
  - name: healthchecks
    database: healthchecks
    password: ref+vault://secret/mongodb#/password
    roles:
      - readWrite
