# Секретные values MinIO — ссылки на Vault (secret/minio)
rootUser: ref+vault://secret/minio#/root_user
rootPassword: ref+vault://secret/minio#/root_password

users:
  - accessKey: ref+vault://secret/minio#/access_key
    secretKey: ref+vault://secret/minio#/secret_key
    policy: healthchecks-rw
